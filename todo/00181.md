# #00181 -- Entire /approval-rate command module is unreachable (not registered); 196 dead lines

**Type:** refactor | **Priority:** Low

executeApprovalRateCommand (approvalRateCommand.ts:39) is the handler for a standalone /approval-rate command. The module is imported by nobody (verified: only self-references in its own header/docstring; no import of 'approvalRateCommand' anywhere in src/web/scripts/tests). 'approval-rate' is not a standalone entry in runtimeManifest -- only '/stats approval-rate' exists (stats/index.ts:50 routes to handleApprovalRate in commands/stats/approvalRate.ts). So this old top-level handler is unreachable; the live approval-rate path is the /stats subcommand, which independently consumes analytics/approvalRate.ts. Dead since the stats consolidation.

**Location:** `src/features/analytics/approvalRateCommand.ts:39`
**Category:** Dead/unreachable code | **Confidence:** high | **Audit severity:** low (adversarially confirmed)

**Suggested fix:** Delete src/features/analytics/approvalRateCommand.ts. Verify no test imports it (none found) and that analytics/approvalRate.ts query functions remain referenced by commands/stats/approvalRate.ts (they are).

**Evidence:** full-codebase multi-agent audit 2026-05-31, finding A0331. See audit/2026-05-31/full-codebase-audit.md.
