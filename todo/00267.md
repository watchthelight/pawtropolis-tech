# #00267 -- Perf pass T5: web dashboard queries and streaming

**Type:** refactor | **Priority:** Medium | **Status:** `WIP`

Sixth tier of the performance plan (dashboard) plus hygiene.

**Items**

1. Backfill page: replace `COUNT(*)` over `messages_archive` and the `dbstat` walk with `backfill_stats` counters and `page_count * page_size`; stream tick 1 s -> 3 s and only on change.
2. Pulse: merged window queries, join instead of correlated `EXISTS`, cached level-role stats.
3. Stats: bounded `claims` CTE, window function in `getModBreakdowns`.
4. Modmail and home: indexed status filter, cached `getModmailStats`, cached `getWhatsNew` per tier and day.
5. Streaming ticket attachments; sharp concurrency 1 on the upload route; precomputed theme replacements in `hooks.server.ts`.
6. System page client poll 5 s -> 15 s.
7. Security fix found during the crawl: whitelist the modmail `filter` query param before it reaches SQL (`web/src/routes/dashboard/modmail/+layout.server.ts`, `web/src/lib/server/queries/modmail.ts`).
8. Hygiene: consolidate cleanup intervals into `rateLimiter`, skip unchanged `user_cache` upserts, pass `type_key` to ticket `emitEvent`.

**Update 2026-09-01:** items 1, 4 (whatsNew and modmail stats cache), 5 (attachment streaming) and 7 landed (b96b725e through acc041b3). The system page already polled every 30 s, so item 6 needed nothing. Not done: the pulse detector rewrites and the stats `claims` CTE bounding (both sit behind 60 s to 5 min caches and their queries use existing indexes on the local copy), sharp concurrency on the upload route, the `hooks.server.ts` regex (about a millisecond per request), and the hygiene list.
