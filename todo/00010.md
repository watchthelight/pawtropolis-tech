# #00010 -- Refactor src/features/serverAuditDocs.ts (1740 lines) into 3 modules

**Type:** refactor | **Priority:** High

serverAuditDocs.ts mixes three concerns: markdown doc generation, security diff analysis, and git commit/push. Tightly coupled in one file. Analyzer functions (analyzeSecurityOnly, getDangerousChanges) cannot be reused without invoking the full git workflow.

Action:
1. Split into src/features/auditDocs/generator.ts (markdown rendering), src/features/auditDocs/analyzer.ts (diff + dangerous-change detection), src/features/auditDocs/vcs.ts (git commit/push).
2. Expose analyzer functions as public API so other audit flows can use them.
3. Add unit tests for analyzer.ts independently of vcs.ts.

**Evidence:** audit/2026-05-19/code-quality-findings.md (Finding 4)
