# #00001 -- Smoke test the new issue system

**Type:** chore | **Priority:** Nominal | **Status:** `WIP`

End-to-end smoke test of the file-first issue system. This todo exists to verify the round trip works: file created, mirrored to a GitHub issue with the right labels, status comment posted on commit activity, file moved to done/, and the GH issue closed.

Acceptance:
- GH issue created with prefix `[00001]`, labels `TODO` + `chore` + `Nominal` + `IP`.
- Body matches the file body after mechanical humanize.
- After a commit referencing `#00001` lands, /issue-update can post a status comment.
- After move to done/, /sync-tasks closes the GH issue with a resolution comment.

**Evidence:** docs/issue-system.md
