# #00012 -- Add web/ API route and dashboard page tests

**Type:** chore | **Priority:** High | **Status:** `WIP`

206 web components and route handlers vs 1 test file (tests/web/linkedRoles.test.ts). Critical API routes (review/profile, modmail/send, sse) and high-use dashboard pages (Reviews, Tickets, Audit) are untested. Large regression surface.

Action:
1. Add API route tests first (higher ROI than component tests). Targets in order:
   - web/src/routes/api/review/profile/+server.ts (DONE 2026-05-20)
   - web/src/routes/api/modmail/send/+server.ts (DONE 2026-05-20)
   - web/src/routes/api/sse/+server.ts (split to #00043, needs ReadableStream + fake-timer harness)
2. Add Vitest config for web/ if not already present.
   - 2026-05-20: added `$lib` alias to shared vitest.config.ts; no separate web config needed.
3. Add dashboard page tests for Reviews, Tickets, Audit.
4. Aim for at least 50% line coverage on web/src/routes/api/.

**Progress 2026-05-20 (slice 1):**
- New helper: `tests/web/_helpers/requestEvent.ts` (minimal RequestEvent factory).
- 23 new tests covering review/profile + modmail/send branches.
- All 4985 tests green; typecheck clean.

**Progress 2026-05-20 (slice 2):**
- New helper: `tests/web/_helpers/db.ts` (in-memory better-sqlite3 seeded from `tests/fixtures/schema.sql`); unblocks DB-bound routes for slice 3.
- 71 new tests across 8 passthrough routes: modmail/open + close + reopen, audit/acknowledge + unacknowledge, art/queue/add + remove, users/resolve.
- Total 5062 tests green; typecheck clean.
- Routes still untested in api/: 26 (14 remaining simple, 4 DB-bound, 4 parameterized, 4 hard).

**Progress 2026-05-20 (slice 3):**
- `makeEvent` extended with `params` field (1-line change): unblocks all routes under `[bracket]/` directories.
- 52 new tests across 3 parameterized routes: scan/[appId]/[scanType] (12), qotd/[action] (18), review/[action] (22).
- Total 5114 tests green; typecheck clean.
- review/[action] tests use the real `canPerformReviewAction` with the real GK role id (`896070888762535969`), pinning the role-vs-tier semantics.
- Routes still untested in api/: 23 (14 simple, 4 DB-bound, 5 misc).

**Progress 2026-05-20 (slice 4):**
- `db.ts` helper extended with the `vi.hoisted` mocking pattern (documented in JSDoc) plus `MISSING_DDL` constants for tables absent from the dumped schema fixture (`channel_cache`, `config_audit_log`).
- 25 new tests across 3 DB-bound routes: health (5), welcome/mentionables (6), export (13).
- Total 5139 tests green; typecheck clean.
- Recon discovered two latent bugs in `web/src/routes/api/export/+server.ts` (wrong table name `audit_results`, missing column `changed_at_s`): spawned #00044 and pinned current throwing behavior in two export tests.
- Routes still untested in api/: 20 (14 simple, 1 DB-bound `tickets/attachments`, 5 misc).

**Progress 2026-05-20 (#00044 closed during slice 4 follow-up):**
- Removed audit + config_audit cases from export handler (no UI consumers; queries referenced nonexistent identifiers).
- Updated 2 export tests, deleted 2 pin tests. Net -2 tests.
- Total 5137 green after close.

**Progress 2026-05-20 (slice 5):**
- 76 new tests across 9 simple passthroughs: art/jobs/{cancel,create,finish,status}, art/queue/{reorder,skip,unskip}, audit/scan/{cancel,start}.
- art/jobs/finish + status tests pin the dual-path auth (sm tier OR SERVER_ARTIST role `896070888749940770`).
- art/queue/reorder pins newPosition=0 as valid (uses `=== undefined` check).
- audit/scan/start pins the auditType allowlist ("members"|"nsfw" only).
- Total 5216 tests green; typecheck clean.
- Routes still untested in api/: 11 (4 DB-bound, 7 external/complex incl. SSE #00043).

**Progress 2026-05-20 (slice 6):**
- 31 new tests across 4 routes: push/subscribe (8), push/unsubscribe (5), art/users/search (8), internal/events (10).
- Three new mock patterns: query-helper module (push, search), HMAC header validation (internal/events), env-cached secret via top-of-file env assignment before dynamic import.
- internal/events tests verify the 64 KB content-length guard, the timing-safe HMAC, and the strict SSEEvent shape (type with colon, finite timestamp, payload field).
- Total 5247 tests green; typecheck clean.
- Routes still untested in api/: 7 (tickets/attachments, config/update, refresh-session, art/jobs/upload, art/uploads/[...path], backfill/stream, sse #00043).

**Progress 2026-05-20 (slice 7):**
- 23 new tests across the two FS + path-traversal routes: tickets/attachments (13), art/uploads/[...path] (10).
- New mock pattern: `vi.mock("node:fs/promises")` to intercept `readFile`. Lets traversal-guard tests assert readFile was NEVER called when a guard fires.
- `db.ts` helper gained `MISSING_DDL.ticket_v067` and `MISSING_DDL.ticket_attachment` (post-067 ticket shape is missing from the schema fixture).
- Spawned #00045 to regenerate the schema fixture (accumulated shims).
- Total 5270 tests green; typecheck clean.
- Routes still untested in api/: 5 (config/update, refresh-session, art/jobs/upload, backfill/stream, sse #00043).

**Evidence:** audit/2026-05-19/code-quality-findings.md (Finding 14)
