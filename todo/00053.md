# #00053 -- Stats Observatory Phase 3: page build with server-rendered SVG charts

**Type:** feat | **Priority:** High | **Status:** `WIP`

Build the page for the Stats Observatory (`todo/00049.md`).

Scope:
1. New isolated route under `web/src/routes/` (path proposed during build) that opts out of the dashboard `+layout` shell and the global theme.
2. `+page.server.ts` loads rollups with tight Cache-Control and considers prerender given the page has no per-user data.
3. `+page.svelte` renders pure HTML plus scoped hardcoded CSS plus server-side SVG charts. Zero hydrating JS, no GSAP, no D3 runtime, no component imports that hydrate.
4. Build the charts as reusable server-rendered SVG snippets.

Acceptance: `web` `npm run check` passes, root `npm run build` passes, page renders all v1 charts server-side.

**Blocked by:** `todo/00051.md`, `todo/00052.md`
