# #00122 -- Hive tests re-implement parsing logic inline and never call detectHive - zero real coverage

**Type:** refactor | **Priority:** Medium

The file's stated purpose is to 'Verify response parsing and error handling' of src/features/aiDetection/hive.ts, but detectHive is never imported or invoked. Every 'parsing' test (lines 40-196) copies the nesting/loop logic into the test body and asserts on that local copy (e.g., lines 58-75, 92-106, 148-160). Config/timeout tests (lines 199-223) assert string/number literals constructed in the test. Because the real detectHive (which performs the fetch, the !response.ok throw at hive.ts:39-42, the output guard at hive.ts:60-64, and the class scan at hive.ts:68-76) is never executed, any regression in the real parser (wrong key path, dropped typeof guard, inverted ok check) passes this suite. This is a test that looks like coverage but provides none for the module it names.

**Location:** `tests/features/aiDetection/hive.test.ts:38-224`
**Category:** dead-code | **Confidence:** high | **Audit severity:** medium (adversarially confirmed)

**Suggested fix:** Mock global.fetch (the file already saves originalFetch at line 27 but never uses it) and call detectHive(url), asserting the returned number/null for the documented response shapes and error codes. Remove or convert the inline-logic 'tests' into real calls.

**Evidence:** full-codebase multi-agent audit 2026-05-31, finding A0177. See audit/2026-05-31/full-codebase-audit.md.
