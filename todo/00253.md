# #00253 -- traceStore clearTraces() and stopAutoPrune() are dead (clearTraces comment claims 'for testing' but no test us

**Type:** refactor | **Priority:** Low

clearTraces (traceStore.ts:118, documented 'Clear all traces (for testing)') and stopAutoPrune (traceStore.ts:135) are exported but never imported anywhere, including tests (verified). startAutoPrune is auto-invoked at module load (line 143) and pruneExpired is used internally by it, so only the externally-facing teardown/test hooks are dead. Harmless but the 'for testing' comment is misleading since no test exercises it.

**Location:** `src/lib/traceStore.ts:118 (clearTraces), 135 (stopAutoPrune)`
**Category:** Unused export | **Confidence:** high | **Audit severity:** low (adversarially confirmed)

**Suggested fix:** Either add a test that uses clearTraces (justifying the comment) or remove clearTraces and stopAutoPrune. Keep startAutoPrune/pruneExpired (live).

**Evidence:** full-codebase multi-agent audit 2026-05-31, finding A0339. See audit/2026-05-31/full-codebase-audit.md.
