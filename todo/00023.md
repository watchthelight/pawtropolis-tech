# #00023 -- Configure eslint env for scripts/ and workers/

**Type:** chore | **Priority:** Medium

ESLint reports ~200 no-undef warnings in scripts/ and workers/discord-proxy/ because env config is wrong. Globals like fetch, __dirname, console, process, Buffer, Response, Request are not declared.

Action:
1. Update .eslintrc (or eslint.config.js flat config) so scripts/ uses `env: { node: true, es2024: true }`.
2. Workers/discord-proxy/ uses Cloudflare Workers globals - add `env: { worker: true }` or service-worker globals.
3. Re-run npm run lint and verify no-undef count drops to near zero.

**Evidence:** audit/2026-05-19/code-quality-findings.md (Finding 25), audit/2026-05-19/deps-ops-findings.md (Finding 5)
