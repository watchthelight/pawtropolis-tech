# #00051 -- Stats Observatory Phase 1: isolated theme design

**Type:** feat | **Priority:** High

Design a fixed, self-contained visual theme for the Stats Observatory (`todo/00049.md`) that looks like nothing else on the site.

Scope:
1. Judge-panel several independent visual directions (for example observatory and astronomy, terminal and CRT, brutalist data-grid, blueprint). Score and synthesize a winner.
2. Output concrete design tokens as hardcoded values with no `var(--hue)` inheritance, scoped to the new route only.
3. Deliver an ASCII or markup mock of the page layout for owner approval.

Constraint: must be gorgeous and load instantly. No dependency on the global theme tokens or the dynamic Discord identity theming.

Acceptance: owner approves one direction; token set and layout mock recorded in the design brief.

**Blocked by:** `todo/00050.md`
