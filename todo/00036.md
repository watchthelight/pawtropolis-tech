# #00036 -- Resolve TODO comment in activityTracker.ts:288 (JSON fallback)

**Type:** chore | **Priority:** Low

src/features/activityTracker.ts:288 has "TODO: Implement JSON fallback logging (future enhancement)" with no tracked issue or timeline.

Action: Choose one:
1. Implement the JSON fallback logging (write to a file when embed logging fails).
2. Remove the feature idea entirely if not pursuing.
3. Convert to a tracked todo (this one), reference its ID in the source comment.

**Evidence:** audit/2026-05-19/code-quality-findings.md (Finding 24)
