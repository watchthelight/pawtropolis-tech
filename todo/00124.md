# #00124 -- Most activityTracker tests assert local literals/SQL strings, not the module under test

**Type:** refactor | **Priority:** Medium

Only the first describe block (lines 59-86) actually calls trackJoin. Everything after line 88 - the UPSERT SQL block (92-102), silent-days math (114-166), config checks (171-197), first-message existence (201-224), embed format (229-258), edge cases (263-295), and schema-column lists (299-330) - constructs strings/objects/arithmetic inside the test and asserts on those, never touching activityTracker.ts. For example the 'uses UPSERT for join tracking' test (92-102) asserts a SQL string defined in the test, while the real trackJoin SQL lives at activityTracker.ts:40-48 and additionally sets `left_at = NULL` on conflict (not present in the test's copy). The silent-days flag logic is re-derived (143-165) instead of calling evaluateAndFlag. These pass unconditionally and cannot detect a real change in the module (e.g., flipping `< config.silentDays` at activityTracker.ts:213, or changing the UPSERT). Note trackFirstMessage/trackLeave/evaluateAndFlag have no real test here at all.

**Location:** `tests/features/activityTracker.test.ts:89-331`
**Category:** dead-code | **Confidence:** high | **Audit severity:** medium (adversarially confirmed)

**Suggested fix:** Drive behavior through the exported functions: assert trackJoin's prepared SQL via the db mock (it can capture the SQL passed to prepare) including the `left_at = NULL` clause, and add tests that call trackFirstMessage to exercise the threshold/flag path. Delete or rewrite the literal-only describe blocks.

**Evidence:** full-codebase multi-agent audit 2026-05-31, finding A0179. See audit/2026-05-31/full-codebase-audit.md.
