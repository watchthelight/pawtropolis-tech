# #00277 -- Perf pass T0: unblock the event loop and stop timer leaks

**Type:** refactor | **Priority:** High | **Status:** `WIP`
nNumbered #00262 in its commits (f4c96ce5 to c1c58b19) and in the 6.2.0 changelog as first written; renumbered to #00277 on 2026-09-02 because #00262 was already closed on 2026-08-29 (claim-all-open button).

First tier of the performance / latency / RAM plan (full plan with file:line evidence lives in the Claude plan file `optimized-nibbling-crane.md`). Everything here is behaviour-preserving except the cadence changes called out.

**Items**

1. opsHealth: `PRAGMA quick_check` ran on the bot connection every 60 s (`src/features/opsHealth.ts`), and `pm2 jlist` was spawned every 60 s (`src/lib/pm2.ts`). quick_check moves to a child process on a 6 h cadence (`src/lib/dbIntegrityCheck.ts`, `src/scheduler/dbIntegrityScheduler.ts`); PM2 status is cached 60 s and polled every 15th tick.
2. `wrapEvent` and `approve.ts withTimeout` never cleared their race timers, retaining handler args for 10 s / 30 s per event.
3. Startup integrity check defaults to `quick` in production (`ecosystem.config.cjs`, `src/lib/dbHealthCheck.ts`).
4. Security audit used `guild.members.fetch()` (full gateway re-chunk) every 30 min; now reads the member cache, runs every 6 h, and skips docs/snapshot/history writes when nothing changed. Same cache substitution in levelStats, artistqueue, threadGate.
5. Reaction archive fetched every partial reaction over REST before recording fields already present on the partial.

**Measurement (M0)**

`src/lib/loopLag.ts` (event-loop delay percentiles in the ops health summary, `loop_lag` warn log) and `scheduler_run` duration logs. Fill in from prod logs:

| Metric | Baseline | After T0 |
|---|---|---|
| loop lag p99 (ms) | | |
| RSS bot (MB) | | |
| boot to ready (s) | | |
| Event handler timeout count per day | | |

**Update 2026-09-01:** all five items landed on branch `perf/optimization-pass` (commits f4c96ce5 through c1c58b19). The measurement hooks are in: `loopLag` percentiles ride in the ops health summary, `scheduler_run` lines carry per-run durations, and `db_integrity_check` lines carry the off-process check duration. The baseline column still has to be filled from production logs after the first deploy; nothing here was measured on the host yet.

## Update 2026-09-01 (second pass)

- Health summary now carries `storage` (data.db bytes, WAL bytes, free-page bytes, archived message count from `backfill_stats`) and `loopLag`; `/dashboard/system` renders both under the integrity line.
- Slow transaction warnings name the call site (file:line) instead of "(anonymous)" so the production ranking in #00270 can be done from the logs.
- Baseline numbers from the host (2026-09-02 probe) are in #00275; the after-deploy columns get filled during the 6.2.0 soak (#00268).

## Baseline vs after (2026-09-02, first 20 minutes after 6.2.0)

| Signal | Before | After 6.2.0 |
|---|---|---|
| slow_transaction (live log files) | 61 lines, max 13157 ms | 0 |
| Event handler timeout | 9 in the probe window | 0 |
| loop_lag warnings | not measured | 0 |
| bot RSS | 262 MB | 222 MB |
| web RSS | 404 MB | 96 MB |
| data.db / WAL | 1636 MB / 180 MB | 1210 MB / 1.5 MB |
| disk used | 83% | 52% |
| pm2 restarts on deploy | 12 each | 13 bot, 14 web (deploy plus VACUUM restart) |
