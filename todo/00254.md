# #00254 -- getHeatmapData() is a dead wrapper superseded by getHeatmapDataForRange()

**Type:** refactor | **Priority:** Low

getHeatmapData(guildId, weeks=1) at heatmap.ts:197 is a thin wrapper whose own docstring (line 195) says callers should 'use getHeatmapDataForRange directly.' The live route web/src/routes/dashboard/heatmap/+page.server.ts:3,21 imports and uses getHeatmapDataForRange, not getHeatmapData. getHeatmapData has no other references (verified). Dead superseded wrapper.

**Location:** `web/src/lib/server/queries/heatmap.ts:197`
**Category:** Unused export (superseded wrapper) | **Confidence:** high | **Audit severity:** low (adversarially confirmed)

**Suggested fix:** Delete getHeatmapData; the route already uses getHeatmapDataForRange.

**Evidence:** full-codebase multi-agent audit 2026-05-31, finding A0341. See audit/2026-05-31/full-codebase-audit.md.
