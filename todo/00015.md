# #00015 -- Re-enable health.ts timeout test

**Type:** chore | **Priority:** Medium

tests/commands/health.test.ts:138-157 has it.skip("handles timeout gracefully with ephemeral message") with a comment about fake-timer + Promise.race difficulty. No tracked issue. Timeout behavior untested.

Action: Choose one:
1. Refactor the health command to avoid Promise.race so fake timers work.
2. Use real timers with a short timeout in the test.
3. Move timeout testing to an integration test.

Reference this todo's ID in the skip comment if you defer.

**Evidence:** audit/2026-05-19/code-quality-findings.md (Finding 6)
