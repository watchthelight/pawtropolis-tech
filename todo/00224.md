# #00224 -- Vacuous tests assert on inline string/boolean literals instead of exercising any source code

**Type:** refactor | **Priority:** Low

The describe blocks 'modmail thread customId format', 'modmail race condition handling', and 'primary key constraint detection' (lines 350-401) construct local literals (e.g. `const customId = ` + tts + `v1:modmail:close:${ticketId}` + tts + `` then expect it to equal `v1:modmail:close:123`; `const isRace = error.message.includes('UNIQUE') || ...`) and assert on those local expressions. They import nothing from threadOpen.ts for these cases and would still pass if the corresponding production logic (customId construction at threadOpen.ts:384, race detection at threadOpen.ts:444-448) were deleted or changed. They provide false confidence in coverage of the customId format and race-detection logic without testing it.

**Location:** `tests/features/modmail/threadOpen.test.ts:350-401`
**Category:** dead-code | **Confidence:** high | **Audit severity:** low (adversarially confirmed)

**Suggested fix:** Either remove these blocks or rewrite them to assert against the real code paths: parse a customId actually emitted by openPublicModmailThreadFor, and drive openPublicModmailThreadFor with a thrown UNIQUE/PRIMARY KEY/SQLITE_CONSTRAINT error to assert the 'already exists' / 'being created by another moderator' branches at threadOpen.ts:450-473.

**Evidence:** full-codebase multi-agent audit 2026-05-31, finding A0195. See audit/2026-05-31/full-codebase-audit.md.
