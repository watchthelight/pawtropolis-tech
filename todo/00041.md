# #00041 -- Sync .env.example with runtime variables

**Type:** chore | **Priority:** Low

.env.example covers Litestream and VAPID but lacks placeholder entries for GOOGLE_VISION_API_KEY, HIVE_API_KEY, SIGHTENGINE_API_SECRET, OPTIC_API_KEY, GITHUB_BOT_TOKEN. New setups will hit cryptic runtime errors.

Action:
1. Add each missing variable to .env.example with a placeholder value and brief description.
2. Verify completeness via:
   ```
   grep -oE '^[A-Z_]+=' .env | cut -d= -f1 | sort > /tmp/actual.txt
   grep -oE '^[A-Z_]+=' .env.example | cut -d= -f1 | sort > /tmp/example.txt
   comm -23 /tmp/actual.txt /tmp/example.txt
   ```
3. The diff should be empty after the fix.

**Evidence:** audit/2026-05-19/deps-ops-findings.md (Finding 19)
