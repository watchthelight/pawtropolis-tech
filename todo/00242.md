# #00242 -- fireAndForget helper is never used anywhere

**Type:** refactor | **Priority:** Low

fireAndForget(promise, label) was added to 'replace the .catch(() => {}) anti-pattern throughout the codebase' (per its own JSDoc), but a repo-wide search of src/, scripts/, web/, workers/, and tests/ finds zero importers or call sites - only the definition itself. The codebase still uses inline .catch(() => undefined)/.catch(err => logger.warn(...)) (e.g. throughout interactionCreate.ts and movie.ts), so the intended adoption never happened. Dead utility module.

**Location:** `src/lib/fireAndForget.ts:1-19`
**Category:** dead-code | **Confidence:** high | **Audit severity:** low (adversarially confirmed)

**Suggested fix:** Either delete src/lib/fireAndForget.ts, or actually adopt it by replacing the scattered fire-and-forget .catch patterns with fireAndForget(...). Prefer deletion unless adoption is planned.

**Evidence:** full-codebase multi-agent audit 2026-05-31, finding A0279. See audit/2026-05-31/full-codebase-audit.md.
