# #00022 -- Run prettier format on web/ (925 drifts)

**Type:** chore | **Priority:** Medium

npm run format:check reports 925 prettier warnings in web/. Compared to 237 in src/ and 106 in tests/. Code review diffs are noisy because of formatting changes.

Action:
1. Run `npm run format -- web/` in one commit.
2. Verify .prettierignore excludes .svelte-kit/ artifacts.
3. Add a pre-commit hook (husky or simple-git-hooks) to run prettier on staged files.
4. Drop the continue-on-error from the Format Check CI step.

**Evidence:** audit/2026-05-19/code-quality-findings.md (Finding 17), audit/2026-05-19/deps-ops-findings.md (Finding 6)
