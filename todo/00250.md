# #00250 -- pruneOldMessages() exported but never called -> message_activity retention never enforced

**Type:** refactor | **Priority:** Low

pruneOldMessages(guildId, daysToKeep=90) is exported but has zero references outside its own file (verified across src/tests/scripts/web, no dynamic refs). This is a retention/cleanup function for the message_activity table that nothing schedules or invokes, so old message-activity rows are never pruned. Low severity as dead code, but note the latent operational consequence: unbounded growth of message_activity (consistent with the 5GB data.db observed).

**Location:** `src/features/messageActivityLogger.ts:198`
**Category:** Unused export | **Confidence:** high | **Audit severity:** low (adversarially confirmed)

**Suggested fix:** Either schedule pruneOldMessages from a periodic task (e.g. the scheduler used for other maintenance) so retention actually happens, or delete it if retention is intentionally handled elsewhere. Do not leave it as an exported no-op.

**Evidence:** full-codebase multi-agent audit 2026-05-31, finding A0334. See audit/2026-05-31/full-codebase-audit.md.
