# #00024 -- Enable tsconfig noUncheckedIndexedAccess and unused checks

**Type:** refactor | **Priority:** Medium | **Status:** `BLOCKED: scope`

tsconfig.json:16 has `noUncheckedIndexedAccess: false`. Lines 13-14 have noUnusedLocals and noUnusedParameters commented out with a TODO. Strict mode is incomplete.

**Investigation 2026-05-19:** Toggled noUncheckedIndexedAccess: true. Typecheck reports 351 errors. Too big for a single tick. Concentrated in web/dashboardApi.ts, linkedRoles.ts, and array-index access throughout src/. Reverted. Tracking remediation as a multi-tick effort. Suggested phasing: (1) fix src/lib/ errors, (2) fix src/web/, (3) fix src/features/, (4) fix src/commands/, (5) finally flip the flag. Each phase ~70 errors.

**Phase 1 done 2026-05-30:** Fixed all 72 src/lib/ errors under the flag (354 total with flag on, down to 282; src/lib now 0). Files: leaderboardImage (39), activityHeatmap (16), modalPatterns (10), time/anomaly/dbHealthCheck/logger/percentiles/reqctx (7). All provably-safe index access (loop bounds, fixed-shape tuples, regex capture groups) resolved with non-null assertions; one += widened to a guarded `?? 0` assignment. Full typecheck (flag off) clean, 5455 tests green. Flag stays false until phases 2-4 land. Remaining under flag: src/features 144, src/commands 103, scripts 12, src/web 10, src/scheduler 8, src/ui 3, others 2.

Action:
1. Enable noUncheckedIndexedAccess: true. Run npm run typecheck and fix any new errors (most will be array-index access requiring a guard).
2. Quick audit of unused vars in src/ via tsc --noEmit with the flags toggled on.
3. Fix any real unused vars, then enable noUnusedLocals: true and noUnusedParameters: true.
4. Remove the TODO comment.

**Evidence:** audit/2026-05-19/deps-ops-findings.md (Finding 7)
