# #00172 -- analytics/queries.ts is transitively dead (only consumer is the dead command.ts)

**Type:** refactor | **Priority:** Low

queries.ts exports getActionCountsByMod, getTopReasons, getVolumeSeries, getLeadTimeStats, getOpenQueueAge. Across src/ the only importer is src/features/analytics/command.ts (itself dead) plus tests/features/analytics/queries.test.ts. The single non-test mention elsewhere (src/db/ensure.ts:267) is a comment, not an import. The live /stats command uses src/features/analytics/approvalRate.ts and src/commands/stats/* queries, not this file. ~528 lines dead.

**Location:** `src/features/analytics/queries.ts:1-528`
**Category:** dead-code | **Confidence:** high | **Audit severity:** low (adversarially confirmed)

**Suggested fix:** Delete src/features/analytics/queries.ts and its test once command.ts is removed. Verify none of stats/* re-imports these names (they do not).

**Evidence:** full-codebase multi-agent audit 2026-05-31, finding A0274. See audit/2026-05-31/full-codebase-audit.md.
