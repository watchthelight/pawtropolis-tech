# #00218 -- Unused afterEach import

**Type:** refactor | **Priority:** Low

`afterEach` is imported from vitest (L8) but never used anywhere in the file (unlike reset.test.ts which uses it for timer teardown). Harmless at runtime but indicates leftover scaffolding; under a strict noUnusedLocals/eslint config this would fail the lint/build gate.

**Location:** `tests/commands/stats/history.test.ts:8`
**Category:** dead-code | **Confidence:** high | **Audit severity:** low (adversarially confirmed)

**Suggested fix:** Remove `afterEach` from the import list.

**Evidence:** full-codebase multi-agent audit 2026-05-31, finding A0172. See audit/2026-05-31/full-codebase-audit.md.
