# #00046 -- Verify production migration state and advance past 045

**Type:** chore | **Priority:** High | **Status:** `BLOCKED: needs prod access`

While regenerating the test schema (#00045), the migration runner was found unable to advance past migration 045 because of five latent bugs (046 default export; 055/058/062/063 malformed recordMigration args). Those are fixed in commit b019b23, but the local data.db is still stamped at version 045, and production (EC2) very likely ran the same broken runner.

If production is also stuck at 045, then migrations 046-077 never applied there. Some of that schema is masked by the startup ensure* self-heal in src/startup/schema.ts, but not all of it: the ticket system (067), config_audit_log (060), channel_cache (053), action_log FTS (072), and the user_cache profile columns (046) are migration-only and may be missing or partial in prod.

Action:
1. On EC2, inspect the live state: `SELECT MAX(version), COUNT(*) FROM schema_migrations;` and check for the post-045 tables (ticket 16/17-column shape, ticket_type, ticket_message, ticket_attachment, ticket_event, ticket_counter, config_audit_log, channel_cache, action_log_fts).
2. If behind, deploy the migration fixes from b019b23 and run `npm run migrate`. Take a backup first (the runner does this automatically). Review the applied-versions table afterward.
3. Confirm the bot features that depend on those tables behave correctly in prod (tickets, dashboard config audit, search).
4. Re-stamp or reconcile the local dev data.db the same way if it is used for anything beyond a migration base.

**Evidence:** #00045 resolution (done/00045.md); commit b019b23 fixed migrations 046/055/058/062/063.
