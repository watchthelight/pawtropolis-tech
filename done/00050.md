# #00050 -- Stats Observatory Phase 0: discovery and metric selection

**Type:** feat | **Priority:** High

Discovery and BMAD diverge-then-converge for the Stats Observatory (`todo/00049.md`).

Scope:
1. Verify the exact column set of every candidate table against both `tests/fixtures/schema.sql` and the live `migrations/` directory. The fixture may lag the migrations.
2. Enumerate every genuinely graphable metric across all domains (moderation, member growth and funnel, message and activity heatmaps, voice and events, engagement, economy and leveling and roles, retention). Rank by signal and visual appeal.
3. Decide the page information architecture and the v1 chart set (heatmaps, ridgelines, growth curves, funnels, leaderboards, sparkline grids, calendar charts), all renderable as server-side SVG or CSS.

Output: a written design brief plus the chosen metric list with SQL sketches and chart-type mapping.

Acceptance: design brief committed under `docs/stats-observatory/`, v1 metric set selected with refresh-cost notes.

**Resolution (2026-05-28):** Phase 0 workflow (16 agents) verified 33 tables against the live migrations, enumerated 56 metrics, and converged on the 14-metric v1 set with a hero-then-drilldowns IA. Brief committed at docs/stats-observatory/design-brief.md (commit 4efe01f). Caught real traps: resonance tables are dead ML output, active_byte_multipliers is snapshot-only, action_log is canonical for decisions.
