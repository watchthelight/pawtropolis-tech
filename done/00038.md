# #00038 -- Remove unused @anthropic-ai/sdk dependency

**Type:** chore | **Priority:** Low | **Status:** `COMPLETE 2026-05-19`

@anthropic-ai/sdk ^0.91.1 in package.json but zero imports found in src/ or scripts/. Adds ~2MB to install plus a chain of transitive dependencies (including vulnerable @protobufjs/utf8).

**Resolution (2026-05-19):** Finding was partially wrong: scripts/llm-label.mjs uses @anthropic-ai/sdk for offline ML labeling (commit 4830c09). Cannot remove without breaking the script. Moved from `dependencies` to `devDependencies` in commit 7ca8d7b. Result: production install no longer pulls the package or its transitive chain. Script still works in dev environment when invoked manually.

Action:
1. Remove @anthropic-ai/sdk from package.json.
2. Run npm ci.
3. Verify no test or runtime path references the package.
4. Single commit: `chore(deps): remove unused @anthropic-ai/sdk`.

**Evidence:** audit/2026-05-19/deps-ops-findings.md (Finding 4)
