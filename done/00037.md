# #00037 -- Fix empty block in scripts/build-overlay-weekly.mjs:100

**Type:** chore | **Priority:** Low | **Status:** `COMPLETE 2026-05-19`

scripts/build-overlay-weekly.mjs:100 has an empty block statement (if/catch with no body). ESLint flags it.

**Resolution (2026-05-19):** Intentional swallow per existing comment on line 97-98 (idempotent ALTER TABLE pattern; SQLite has no ADD COLUMN IF NOT EXISTS). Added inline comment `/* column already exists */` inside the catch to satisfy eslint no-empty rule. Commit e9bb0bf.

Action:
1. Open the file at line 100.
2. Either remove the empty block if unreachable, or add a comment explaining why it is intentional.
3. Re-run npm run lint to confirm the error is gone.

**Evidence:** audit/2026-05-19/code-quality-findings.md (Finding 26)
