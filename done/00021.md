# #00021 -- Replace lazy any types in src/features/opsHealth.ts

**Type:** refactor | **Priority:** Medium | **Status:** `COMPLETE 2026-05-19`

src/features/opsHealth.ts uses : any in 12 places for health-check payloads. Cannot safely refactor or extend health logic.

**Resolution (2026-05-19):** Actual count was 10 `catch (err: any)` plus 2 `Record<string, any>` for meta fields (12 total - audit was right). Replaced (commit e7e46d5):
- All `catch (err: any)` -> `catch (err)` (TS5 default `unknown`).
- Added inline helper `errMsg(err: unknown): string` mirroring pattern from src/lib/dbHealthCheck.ts:73.
- `err.message` -> `errMsg(err)`.
- `Record<string, any>` -> `Record<string, unknown>` on alert meta (forces narrowing on consumer side).

Result: 0 `any` in opsHealth.ts. Typecheck clean.

Action:
1. Define strict types for each health payload (db health, scheduler health, disk space, network).
2. Replace each `: any` with the proper interface.
3. Run npm run typecheck to confirm.

**Evidence:** audit/2026-05-19/code-quality-findings.md (Finding 16)
