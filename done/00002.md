# #00002 -- Patch HIGH and CRITICAL npm vulnerabilities

**Type:** security | **Priority:** Critical | **Status:** `COMPLETE 2026-05-19`

npm audit --production reports a CRITICAL protobufjs RCE plus HIGH-severity issues in fastify (content-type validation bypass + X-Forwarded spoofing), undici (HTTP smuggling and WebSocket overflows), lodash (code injection via _.template), minimatch (ReDoS), fast-uri (path traversal), and jws (HMAC signature bypass).

**Resolution (2026-05-19):** Closed all HIGH and CRITICAL in production audit (root + web/). Steps landed across 5 commits:
1. d7d3df5 removed @xenova/transformers (kills the @xenova->onnxruntime->protobufjs chain).
2. 877d6ab upgraded fastify ^5.7.4 -> ^5.8.5 (content-type bypass + X-Forwarded spoofing).
3. da26055 upgraded discord.js ^14.16.3 -> ^14.26.4 (pulls non-vulnerable undici).
4. f24dd7b added root overrides: protobufjs >=7.5.8 (fixes Google Vision SDK chain), fast-uri >=3.1.2, jws >=4.0.1, minimatch >=9.0.7, brace-expansion >=2.0.3, @tootallnate/once >=3.0.1, ws >=8.20.1.
5. 351fbad upgraded web vite 7.3.1 -> 7.3.3, @sveltejs/kit ^2.50.2 -> ^2.60.1, svelte ^5.51 -> ^5.55.8, plus web overrides for picomatch/brace-expansion.

Final state: `npm audit --omit=dev` -> 0 vulnerabilities at root; 0 HIGH and 0 CRITICAL in web/. Dev-only stragglers remain (eslint chain) but do not ship.

Action:
1. Run `npm audit fix` on root and `web/`.
2. Upgrade fastify to >= 5.8.5 (covers content-type + X-Forwarded).
3. Upgrade discord.js to latest 14.x (pulls non-vulnerable undici) or pin undici >= 6.24.0.
4. Upgrade lodash to >= 4.17.24.
5. Upgrade minimatch to >= 9.0.7.
6. Re-run `npm audit` and verify HIGH count is zero.
7. Run `npm run typecheck && npm run test && npm run build` to validate.

**Evidence:** audit/2026-05-19/security-findings.md (Finding 2), audit/2026-05-19/deps-ops-findings.md (Findings 2, 3)
