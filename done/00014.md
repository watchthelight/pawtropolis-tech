# #00014 -- Refactor hasStaffPermissions for testability

**Type:** chore | **Priority:** Medium | **Status:** `COMPLETE 2026-05-19`

tests/lib/config.test.ts has describe.skip("hasStaffPermissions") with four unimplemented it.todo tests because hasStaffPermissions uses require() internally that vi.mock cannot intercept. Staff permission logic is central to access control but untested.

**Resolution (2026-05-19):** Verified the original premise was wrong: `hasStaffPermissions` does NOT use require() (no `require(` anywhere in src/lib/config.ts). It calls `isOwner`, `isGuildMember`, `hasManageGuild`, `isReviewer` only. The vi.mock setup at the top of config.test.ts already covers the deps (db, logger, env, isOwner). No code refactor was needed.

Removed `describe.skip` framing in commit 795eba3. Implemented all 5 tests (originally 4 it.todo + 1 null member case):
- bot owner -> true
- ManageGuild -> true
- reviewer via configured role -> true
- non-staff member -> false
- null member -> false

All 26 tests pass in tests/lib/config.test.ts (5 new + 21 existing).

Action:
1. Refactor hasStaffPermissions to accept dependencies (guildMember, client) as arguments rather than require() them internally.
2. Un-skip the test suite.
3. Implement the four it.todo tests covering: caller is staff, caller is not staff, caller's roles missing, guild not found.

**Evidence:** audit/2026-05-19/code-quality-findings.md (Finding 8)
