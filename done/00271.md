# #00271 -- Dead code and dependency audit with knip; script index

**Type:** chore | **Priority:** Medium

No dead-export tooling exists in the repo. `src/` holds 346 TypeScript files across commands, features, lib, scheduler and web, plus 70 scripts, several of them one-offs from 2025-11 with no references.

**Plan**

1. Add `knip` as a dev dependency with a config covering `src/index.ts`, `scripts/*.ts`, `scripts/*.mjs`, `migrations/*.ts`, the SvelteKit routes and components, and the vitest setup.
2. Triage its report into delete, keep-with-reason, and false positive (config). Delete in small commits per area with the knip evidence in the commit body.
3. Remove unused dependencies it reports in both package.json files.
4. Delete `scripts/generate-glb-models.cjs`, `scripts/check-thread.mjs`, `scripts/BACKFILL-NOW.sh`, `scripts/CLEANUP-ALL.sh` after reading each once; read the commit messages of `scripts/_codemod-tokens.cjs`, `_introspect.cjs`, `_mint-session.cjs`, `fix-review-action.cjs` and delete the finished one-offs.
5. `scripts/README.md` groups what remains: operations, badges, local research (the chat-quality pipeline needs embed tables that production does not have), one-shot maintenance.
6. `npm run knip` in `package.json` and as a soft CI step; `docs/operations/ci-policy.md` updated.

## Update 2026-09-01

- knip 6.34 installed with `knip.json` (root and `web` workspaces). First report: 0 unused files, 196 unused exports and 103 unused types across 60 files, `pino-pretty` flagged (it is loaded by name as a pino transport, so it is on the ignore list), `@sveltejs/kit` unlisted for a test helper (ignored, resolved from web).
- `knip --fix` stripped the export keywords; a TypeScript-API script then deleted every declaration `tsc` reported as unused (61 plus 7 in a second pass). Committed per area with the report as evidence. `npm run typecheck`, `npm test`, `npm run build` and `cd web && npm run check` all pass.
- 20 finished one-off scripts deleted, one commit each with last-touch evidence: CLEANUP-ALL.sh, BACKFILL-NOW.sh, generate-glb-models.cjs, check-thread.mjs, verify-commands.mjs, verify-review-action.cjs, ack-issues.ts, backfill-user-cache.ts and .mjs, setup-level-rewards.ts, post-migration-modmail-notices.ts, stamp-migrations.mjs, run-migration-046.mjs, backfill-patreon-art-grants.ts, assign-leadership-tags.mjs, fix-review-action.cjs, _introspect.cjs, _mint-session.cjs, _codemod-tokens.cjs, perms-recon.ts.
- `scripts/README.md` indexes what remains (operations, badges, diagnostics, local-only research pipeline, maintenance tools). `npm run knip` is a soft CI step; promotion condition recorded in `docs/operations/ci-policy.md`.

## Closed 2026-09-02

knip adopted as a soft CI step, 196 exports and 103 types removed, 20 one-off scripts deleted, scripts/README.md indexes the rest.
