# #00186 -- Unguarded .get().sql throws opaque TypeError when general_messages_score is absent

**Type:** bug | **Priority:** Low

Same defect as export-embed.mjs line 13: `src.prepare(...).get().sql` dereferences undefined and throws an opaque TypeError if the source table is missing, instead of a clear message. Trusted-local-data tool, so impact is operator confusion only.

**Location:** `scripts/export-score.mjs:13`
**Category:** correctness | **Confidence:** high | **Audit severity:** low (adversarially confirmed)

**Suggested fix:** Guard with `const row = ...get(); if (!row?.sql) { ...exit with message... }` before dst.exec.

**Evidence:** full-codebase multi-agent audit 2026-05-31, finding A0018. See audit/2026-05-31/full-codebase-audit.md.
