# #00048 -- Patch current npm audit vulnerabilities

**Type:** chore | **Priority:** High

`npm audit` reports two live vulnerabilities, both with non-breaking fixes:
- rollup 4.0.0-4.58.0: arbitrary file write via path traversal (HIGH, GHSA-mw96-cpmx-2vgc).
- ajv: ReDoS when using the `$data` option (moderate).

Both are dev / build-chain transitive dependencies (rollup arrives via vitest + vite). This is the present-day form of security audit finding 2 (the original Critical/High deps -- fastify, undici, lodash, minimatch -- are already patched).

Action:
1. Run `npm audit fix` (non-breaking; no `--force`).
2. Verify typecheck + full test suite still pass (vitest exercises rollup).
3. Commit the lockfile change.

**Evidence:** audit/2026-05-19/security-findings.md (Finding 2); `npm audit` on 2026-05-27.

**Resolution (2026-05-27):** `npm audit fix` bumped rollup 4.52.4 -> 4.60.4 (within major 4) and patched ajv; package.json unchanged (transitive only). `npm audit` now reports 0 vulnerabilities. typecheck clean, 5426 tests pass. Commit on this branch (see chore(deps) commit referencing #00048).
