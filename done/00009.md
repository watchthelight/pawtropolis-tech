# #00009 -- Refactor src/commands/audit.ts (1760 lines) into subcommand modules

**Type:** refactor | **Priority:** High

/audit command has 1,760 lines covering 5 separate workflows (members, nsfw, findings, acknowledge, etc.). Each subcommand inlines its own multi-step interaction flow with progress reporting, embed generation, button creation, and db queries.

Action:
1. Create src/commands/audit/ subdirectory.
2. One file per subcommand: audit/members.ts, audit/nsfw.ts, audit/findings.ts, audit/acknowledge.ts.
3. Extract shared workflows (progress reporting, audit button router, embed builders) to audit/shared.ts.
4. src/commands/audit.ts becomes a dispatcher that loads the registry and routes.

**Evidence:** audit/2026-05-19/code-quality-findings.md (Finding 3)

**Resolution (2026-05-27):** audit.ts reduced 1760 -> 115 lines, now a dispatcher. Subcommands extracted to src/commands/audit/ (members, nsfw, confirm, acknowledge, acknowledgeAll, unacknowledge, security, trends, diff) with shared.ts, buttonRouter.ts, and data.ts. Commits 6d7cd44 (slice A: data + shared), bbb2d15 (slice B-C: dispatcher + members/nsfw/confirm/button). Typecheck + suite green.
