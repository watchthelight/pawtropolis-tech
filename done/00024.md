# #00024 -- Enable tsconfig noUncheckedIndexedAccess and unused checks

**Type:** refactor | **Priority:** Medium

tsconfig.json:16 has `noUncheckedIndexedAccess: false`. Lines 13-14 have noUnusedLocals and noUnusedParameters commented out with a TODO. Strict mode is incomplete.

**Investigation 2026-05-19:** Toggled noUncheckedIndexedAccess: true. Typecheck reports 351 errors. Too big for a single tick. Concentrated in web/dashboardApi.ts, linkedRoles.ts, and array-index access throughout src/. Reverted. Tracking remediation as a multi-tick effort. Suggested phasing: (1) fix src/lib/ errors, (2) fix src/web/, (3) fix src/features/, (4) fix src/commands/, (5) finally flip the flag. Each phase ~70 errors.

**Phase 1 done 2026-05-30:** Fixed all 72 src/lib/ errors under the flag (354 total with flag on, down to 282; src/lib now 0). Files: leaderboardImage (39), activityHeatmap (16), modalPatterns (10), time/anomaly/dbHealthCheck/logger/percentiles/reqctx (7). All provably-safe index access (loop bounds, fixed-shape tuples, regex capture groups) resolved with non-null assertions; one += widened to a guarded `?? 0` assignment. Full typecheck (flag off) clean, 5455 tests green.

**Phase 2 done 2026-05-30:** Fixed all 10 src/web/ errors (282 -> 272 under flag; src/web now 0). Files: badgeEndpoint (regex capture + destructured group), linkedRoles (loop-bounded entries[i]), routes/config (firstError guarded by `if (!valid)`), routes/health (df parse inside `if (lines.length >= 2)`), routes/review (mentions[0] inside `length === 1`). All provably-safe, resolved with non-null assertions, no runtime change. Full typecheck (flag off) clean, 5455 tests green. Flag stays false until phases 3-4 land. Remaining under flag: src/features 144, src/commands 103, scripts 12, src/scheduler 8, src/ui 3, src/handlers 1, src/events 1.

**Phase 3 in progress 2026-05-30:** src/features (144 errors) being cleared file-by-file. Batch 1 done: serverAudit/analyze.ts (17, loop-bounded sortedRoles[i]/[j] role-hierarchy pairs), modPerformance.ts (3, calculatePercentile clamped index widened to `?? null`, latestSubmission from non-empty filtered array), analytics/queries.ts (4, p50/p90/max_age percentile reads guarded by empty-result early returns + clamped indices). 272 -> 248 under flag; src/features 144 -> 120. Verified: scoped tsc per file = 0, full typecheck (flag off) clean, 5455 tests green. Remaining feature hotspots: review/handlers/buttons (15), levelRewards (13), serverAudit/docs (12), badges/snapshotResolve (9), gate/dmVerification (7), patreonArtRewards (7), events/gameNight (7), and ~13 smaller files. NOTE: feature-layer undefined is often a REAL possibility (map/.find/DB rows) -- read each site, do not mechanically apply `!`.

**Phase 3 batch 2 done 2026-05-30:** levelRewards.ts (13, `reward = rewards[i]` in a `for (i < rewards.length)` grant loop after a zero-length early return; single assertion clears all 13 cascading reward.* accesses) and badges/snapshotResolve.ts (9, regex `.exec()` capture-group reads `hierMatch[1]`/`idsMatch[1]`/`m[1]`/`m[2]` all guarded by `if (match)` or `if (!m) continue` with fixed group counts). 248 -> 235 under flag; src/features 120 -> 98. Verified: scoped tsc per file = 0, full typecheck (flag off) clean, 5455 tests green.

**Phase 3 batch 3 done 2026-05-30:** serverAudit/docs.ts (12, same loop-bounded sortedRoles[i]/[j] hierarchy-inversion pattern as analyze.ts; 2 assertions clear all 12) and gate/dmVerification.ts (7: currentQuestionIndex/nextIndex/startIndex reads are session-invariant or branch-guarded so asserted; pages[0] in the modal fallback asserted. PLUS a real bug fix: added an early `if (questions.length === 0)` guard before reading questions[startIndex] -- an admin with zero configured questions would previously have hit a runtime crash. The guard responds gracefully and returns). 235 -> 214 under flag; src/features 98 -> 79. Verified: scoped tsc per file = 0, full typecheck (flag off) clean, 5455 tests green.

**Phase 3 batch 4 done 2026-05-30:** review/handlers/buttons.ts (14 of 15 in this commit; the 15th was finished in batch 5). Regex capture-group reads guaranteed present after the enclosing `if (!match) return` guard, across the review button handlers: handleReviewButton `[, action, code]` destructure -> indexed asserts, plus modmail/permReject/voteOut `match[1]`/`match[2]` and the ping handler's legacy payload + userMatch. Verified: full typecheck (flag off) clean, 5455 tests green.

**Phase 3 batch 5 done 2026-05-30:** review/handlers/buttons.ts (last site: handleCopyUidButton `[, code, userId]` destructure -> indexed asserts), patreonArtRewards.ts (7, `artType = artTypes[i]` loop-bounded over Object.keys, single assert clears the index-type + arg errors), events/gameNight.ts (7: `key.split(":")` on `guildId:userId` keys narrowed via `as [string, string]` since split always yields 2 parts; ordinal `s[0]` fallback asserted; `[...entries()][0]` asserted after an emptiness guard above). 200 -> 185 under flag; src/features 65 -> 50. Verified: scoped tsc per file = 0, full typecheck (flag off) clean, 5455 tests green. Remaining feature files: review/handlers/modals (6), aiDetection/index (6), review/card (5), patreonRoleDedup (5), movieNight (5), dbRecoveryButtons (4), statsObservatory/refresh (3), modmail/handlers (3), and ~8 smaller.

**Phase 3 batch 6 done 2026-05-30:** review/handlers/modals.ts (6, identical `code = match[1]` reads after `if (!match) return` guards across the reject/accept/kick/unclaim/permreject/voteout modal submit handlers) and aiDetection/index.ts (6: `results[i]` in detectAIForImage maps 1:1 over enabledServices via the same .map index, and `results[i]` in buildAIDetectionEmbed is a for-loop bounded by results.length; all asserted). 185 -> 173 under flag; src/features 50 -> 38. Verified: scoped tsc per file = 0, full typecheck (flag off) clean, 5455 tests green.

**Phase 3 batch 7 done 2026-05-30:** movieNight.ts (5: `key.split(":")` on `guildId:userId` keys narrowed to `[string, string]` at two loops; ordinal `s[0]` fallback asserted; recovery-path `[...activeEvents.entries()][0]` asserted after the existing emptiness guard), patreonRoleDedup.ts (5, `highest = memberPatreonRoles[0]` after `if (length <= 1) return` so >=2 elements; single assert clears all 5 cascading `highest.*` reads), review/card.ts (5, `probe = rows[0]` after `if (rows.length === 0) return`; single assert clears all 5 cascading `probe.*` reads). 173 -> 158 under flag; src/features 38 -> 23. Verified: scoped tsc per file = 0, full typecheck (flag off) clean, 5455 tests green. Remaining feature files (~13): dbRecoveryButtons (4), modmail/handlers (3), statsObservatory/refresh (3), gate/handlers (2), qotd/handlers (2), byteTokenHandler (2), artistRotation/handlers (1), artJobs/store (1), badges/color (1), botDetection (1), review/handlers/actionRunners (1), welcome (1), welcomeBatch (1).

**Phase 3 batch 8 done 2026-05-30 (src/features COMPLETE):** Cleared the final 23 errors across 13 files. All regex `.exec()` capture reads (botDetection, badges/color, modmail, qotd) and `match` destructures (dbRecoveryButtons, qotd) asserted after their `if (!match) return` guards; length-guarded array reads asserted (artJobs/store rows[0] in `length===1`, gate/handlers pages[requestedPage]/missingRequired[0], actionRunners mentions[0], welcome/welcomeBatch users[0] in `length===1`); loop-bounded reads in statsObservatory/refresh (dayList[i], dayList[i-7] in `i>=7`, TENURE_BUCKETS[i]). Two source-level type tightenings: artistRotation/handlers ParsedRedeemCustomId fields made required (the `parts.length < 6` guard guarantees them) so handleConfirm gets non-optional strings; byteTokenHandler return confirmId/userId asserted after `parts.length < 4` guard. **src/features cleared to 0.** Verified: full typecheck (flag off) clean, 5455 tests green. (Correction: a follow-up commit finished 3 stragglers in artistRotation/handlers and modmail/handlers whose edits had not landed in the batch commit; src/features is now genuinely 0. Under-flag total is 128, not the 120 first written here -- src/scheduler is 8, not 1.) PHASE 3 COMPLETE. Remaining under flag (128): src/commands 103, scripts 12, src/scheduler 8, src/ui 3, src/handlers 1, src/events 1. Next: Phase 4 (src/commands), then scripts + small dirs, then flip the flag.

**Phase 4 done 2026-05-31 (src/commands COMPLETE):** Cleared all 103 src/commands errors across 4 batches: audit (trends/buttonRouter/diff, 32), isitreal/config/verify/database/attendance/flag/usebyte/update (~24), stats/listopen/art (23), event/movie/help (~25). Provably-safe assertions for loop-bounded reads, length-guarded reads, regex capture groups, and union-typed Record lookups (viewMode, ACTIVITY_TYPE_MAP whose choices match the map keys). Three real guards added in config/isitreal.ts: unknown-service lookups from raw customId strings now reply "Unknown service" and return instead of crashing on info.name.

**Phase 5 done 2026-05-31 (src + scripts COMPLETE):** Cleared the remaining 25: src/scheduler (byteMultiplier settled[i]/expired[i] loop-bounded, staleApplicationCheck staleApps[0] guarded by escalationApps.length>0 + guildApps[0] push-built), src/ui/reviewCard (fixed-array ordinal s[0], qa loop-bounded), src/handlers/errorCardButtons + src/events/messageCreate (capture group + length-guarded), and scripts (perms-recon, generate-badges, migrate, fetch-role-data, deploy-commands). Under-flag total reached 0.

**Phase 6 done 2026-05-31:** Flipped noUncheckedIndexedAccess: true. Full verify green: npm run typecheck 0 errors, 5455 tests pass, npm run build success.

**Phase 7 done 2026-05-31 (COMPLETE):** Enabled noUnusedLocals + noUnusedParameters and removed the TODO comment. 114 unused-symbol errors (106 TS6133, 8 TS6196) cleared: an 8-agent workflow swept disjoint file groups (unused imports removed, dead non-exported decls deleted, side-effecting locals kept as bare calls, unused params underscore-prefixed, unused destructured props dropped), then 7 cascade stragglers fixed by hand (orphaned imports/types after decl removals). Notable: googleVision.ts legacy loadVisionClient (40 lines of self-documented dead SDK path) removed; interactionCreate.ts write-only `succeeded` flag + its 52 assignments removed. Full verify green: typecheck 0, 5455 tests, build success. **TypeScript strict mode is now complete.**

**Evidence:** audit/2026-05-19/deps-ops-findings.md (Finding 7)
