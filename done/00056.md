# #00056 -- Patch web moderate npm audit vulnerabilities

**Type:** chore | **Priority:** Medium

`npm --prefix web audit` reports 7 vulnerabilities (5 low, 2 moderate) as of 2026-05-31.

Two moderate issues have clean, non-breaking transitive fixes:
- `postcss` XSS via unescaped `</style>` in CSS stringify output.
- `brace-expansion` DoS (large numeric range defeats the documented max protection).

The remaining 5 low issues live in the `@sveltejs/kit` -> `cookie` dependency chain (cookie accepts out-of-bounds characters in name/path/domain). npm's only suggested fix is a destructive downgrade of `@sveltejs/adapter-auto` to 0.0.1 (flagged isSemVerMajor), which is not a real fix. Those resolve only when SvelteKit ships an updated pinned `cookie`. Leave them; do not apply the downgrade.

Action:
1. Run `npm --prefix web audit fix` (no `--force`) to patch the 2 moderate transitives only.
2. Verify `npm --prefix web run check` and the web build still pass.
3. Confirm `npm --prefix web audit` now shows only the 5 low cookie-chain items.

**Done 2026-05-31:** `npm --prefix web audit fix` (no `--force`) bumped the postcss and brace-expansion transitives; only `web/package-lock.json` changed. Web `npm run check` baseline unchanged (15 pre-existing errors, no new) and `npm run build` succeeds. `npm --prefix web audit` now reports 5 low only, all in the `@sveltejs/kit` -> `cookie` chain with no non-destructive fix; left for a future SvelteKit bump. Root audit remains 0.

**Evidence:** npm --prefix web audit 2026-05-31.
