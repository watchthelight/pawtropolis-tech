# #00003 -- Remove or isolate @xenova/transformers (protobufjs RCE)

**Type:** security | **Priority:** Critical | **Status:** `COMPLETE 2026-05-19`

@xenova/transformers ^2.17.2 pulls a vulnerable protobufjs that allows RCE via code injection. No `import` references found in src/ or web/src/, suggesting the package is unused.

**Resolution (2026-05-19):** Confirmed unused via grep across src/, scripts/, web/src/, tests/ (zero references). NSFW avatar feature uses Google Cloud Vision exclusively (`src/features/avatarScan.ts:132` dynamically imports `./googleVision.js`). Removed from package.json in commit d7d3df5. Result: 32 transitive packages removed; @xenova->onnxruntime->protobufjs chain severed.

Note: protobufjs still reachable via @google-cloud/vision; addressed separately in #00002 via `overrides` block (>=7.5.8).

Action:
1. Search runtime paths (lazy imports, dynamic require) to confirm zero usage.
2. If confirmed unused, remove from package.json and run npm ci.
3. If used (perhaps via a worker / runtime path not detected by grep), downgrade to 2.0.1 or isolate to a separate process so the RCE surface is bounded.
4. Re-run npm audit to confirm protobufjs removed.

**Evidence:** audit/2026-05-19/deps-ops-findings.md (Finding 1)
