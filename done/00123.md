# #00123 -- Optic tests re-implement parsing inline and never call detectOptic - zero real coverage

**Type:** refactor | **Priority:** Medium

Same pattern as the Hive test. The file claims to verify src/features/aiDetection/optic.ts response parsing/error handling, but detectOptic is never imported or called. The 'parsing' tests (lines 33-101) just read `response?.report?.ai?.confidence` from local literals; the API-config tests (lines 105-123) assert locally-built strings; the 'non-numeric confidence' test (lines 91-101) only checks `typeof x === 'number'` on a local object, not optic.ts's actual guard at optic.ts:57-60. The real function's behavior (Bearer header, `object` body key at optic.ts:32-34, !response.ok throw, null on non-numeric) is never exercised, so regressions in optic.ts would not fail this suite.

**Location:** `tests/features/aiDetection/optic.test.ts:26-140`
**Category:** dead-code | **Confidence:** high | **Audit severity:** medium (adversarially confirmed)

**Suggested fix:** Mock fetch and invoke detectOptic(url); assert the numeric/null return and that the request used the `object` body key and Bearer auth. Replace inline-logic assertions with real calls.

**Evidence:** full-codebase multi-agent audit 2026-05-31, finding A0178. See audit/2026-05-31/full-codebase-audit.md.
