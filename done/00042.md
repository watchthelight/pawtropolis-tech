# #00042 -- Clean up stale .env backup files

**Type:** chore | **Priority:** Low | **Status:** `COMPLETE 2026-05-19`

.env.build (May 15), .env.migration-backup (Mar 1), .env.pre-new-bot.bak (Apr 8) sit in the working tree. Backups for ad-hoc migrations that are months old.

**Resolution (2026-05-19):** Verified none committed (`git ls-files | grep ^\.env` -> only .env.example). Deleted all three local files. .gitignore already covers them via `.env.*` pattern (line 12), so no .gitignore change needed.

Action:
1. Verify none are committed (`git ls-files | grep ^\.env`).
2. Delete local copies.
3. Add patterns .env.*.bak, .env.backup*, .env.build to .gitignore if not already covered.
4. If future migrations need backups, write them to a directory outside the repo (e.g. ~/pawtropolis-env-backups/).

**Evidence:** audit/2026-05-19/deps-ops-findings.md (Finding 9)
