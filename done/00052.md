# #00052 -- Stats Observatory Phase 2: rollup migration, refresh script, query module

**Type:** feat | **Priority:** High

Build the precompute data layer for the Stats Observatory (`todo/00049.md`).

Scope:
1. Author a new migration (next number is `078`) for the rollup table or tables (for example `daily_metrics` plus any needed aggregates). Add the same tables to `tests/fixtures/schema.sql` so tests have them.
2. Write a refresh script under `scripts/` that runs the heavy GROUP BY aggregations and populates the rollups. Wire an `npm run stats:refresh` entry.
3. Write a read-only query module under `web/src/lib/server/queries/` that selects only from the rollups, matching the existing WAL read-only pattern in `web/src/lib/server/db.ts`.
4. Unit and integration tests against the `tests/fixtures/schema.sql` seed.

Note: production application of migration 078 depends on the prod migration runner being unblocked (`todo/00046.md`). Local tests use the fixture schema and are unaffected.

Acceptance: typecheck green, tests green, query module returns rollup data only.

**Blocked by:** `todo/00050.md`

**Resolution (2026-05-28):** Migration 078 (ten rollup tables) plus tests/fixtures/schema.sql, the refreshPublicStats module plus `npm run stats:refresh` CLI, and a rollup-only query module. Aggregation correctness pinned by tests/features/statsObservatory/refresh.test.ts (11 cases); typecheck and 5453 tests green. Commits 5e983ea, aa4c1b6, bcc3ae5, ca546a0.
