# #00015 -- Re-enable health.ts timeout test

**Type:** chore | **Priority:** Medium | **Status:** `COMPLETE 2026-05-19`

tests/commands/health.test.ts:138-157 has it.skip("handles timeout gracefully with ephemeral message") with a comment about fake-timer + Promise.race difficulty. No tracked issue. Timeout behavior untested.

**Resolution (2026-05-19):** Re-enabled the test in commit 0ea5b74. Root cause of original skip was a stale assertion: test expected `timeoutCall.ephemeral === true`, but production code uses `flags: MessageFlags.Ephemeral` (the modern Discord.js pattern). Updated:
- Replaced `mockImplementation(...)` with `mockImplementationOnce(...).mockResolvedValueOnce(...)` so the second call (timeout fallback) resolves cleanly.
- Replaced `(client as any).ws` with a properly typed cast.
- Assertion now checks `timeoutCall.flags` is defined instead of legacy `.ephemeral`.

Fake timers + Promise.race work fine; no production refactor needed. All 7 tests pass (including the previously-skipped one).

Action: Choose one:
1. Refactor the health command to avoid Promise.race so fake timers work.
2. Use real timers with a short timeout in the test.
3. Move timeout testing to an integration test.

Reference this todo's ID in the skip comment if you defer.

**Evidence:** audit/2026-05-19/code-quality-findings.md (Finding 6)
