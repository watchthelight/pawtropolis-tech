# #00047 -- Un-skip flag.store.test.ts and cover flagsStore CRUD

**Type:** chore | **Priority:** Medium

tests/flag.store.test.ts was a `describe.skip` placeholder: the original tests were dropped because src/store/flagsStore.ts prepared statements at module load, before vi.mock could intercept the db symbol. Flag CRUD (security-relevant moderation state) had zero coverage.

Action:
1. Confirm flagsStore lazy-initializes prepared statements (so the db mock is intercepted on first use).
2. Replace the placeholder with real tests against an in-memory better-sqlite3.
3. Cover getExistingFlag, isAlreadyFlagged, getFlaggedUserIds, upsertManualFlag.

**Evidence:** audit/2026-05-19/code-quality-findings.md (Finding 7).

**Resolution (2026-05-27):** flagsStore had already been refactored to lazy-init statements (_stmtCache + lazyStmt), so no production change was required. Replaced the skipped placeholder with 13 tests covering all four CRUD functions: flagged/unflagged/guild-user scoping, the auto-flag override path, joined_at fallback on insert, update-in-place (no duplicate row), and reason trim+truncate to FLAG_REASON_MAX_LENGTH (512). Full suite green (5425, no skips), typecheck clean. Commit c4df0f6.
