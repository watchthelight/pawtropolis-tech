# #00020 -- Replace lazy any types in scripts/commands.ts

**Type:** refactor | **Priority:** Medium | **Status:** `COMPLETE 2026-05-19`

scripts/commands.ts:50-180 uses : any in 16 places for Discord.js command builders and deserialized payloads. Makes refactoring this script unsafe.

**Resolution (2026-05-19):** Replaced 16 `any` uses with proper types in commit 08999be:
- `opt`/`sub`: `APIApplicationCommandOption` (already aliased as CmdOption).
- `command`: `CommandSpec[number]`.
- `commands: any[]`: `CommandSpec`.
- REST PUT response: `as CommandSpec`.
- REST GET response: `as CmdOption[]`.
- `catch (err: any)`: `catch (err: unknown)` with `err as { code?: number; status?: number }` narrowing at access sites.
- Union-type narrowing on .options via `"options" in sub ? sub.options : undefined` pattern (since not all CmdOption members have .options).

Result: 0 any in scripts/commands.ts. Typecheck clean.

Action:
1. Import Discord.js command builder types (SlashCommandBuilder, SlashCommandSubcommandBuilder, etc.).
2. Define a local CommandPayload interface for the deserialized JSON.
3. Replace each `: any` with the appropriate type.
4. Run npm run typecheck to confirm.

**Evidence:** audit/2026-05-19/code-quality-findings.md (Finding 15)
